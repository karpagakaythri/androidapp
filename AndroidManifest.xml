<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ruralapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.WithActionBar">
        <activity
            android:name="com.example.ruralapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.ruralapp.CustomPref"></activity>
        <activity android:name="com.example.ruralapp.Regn" android:label="Register"></activity>
        <activity android:name="com.example.ruralapp.UserHome" android:label="Home"></activity>
        <activity android:name="com.example.ruralapp.Complaints" android:label="Complaint"></activity> 
        <activity android:name="com.example.ruralapp.ViewComp" android:label="Complaint Status"></activity>
    </application>
</manifest>
